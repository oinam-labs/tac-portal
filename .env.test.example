# =============================================================================
# TAC Cargo Portal - Test Environment Variables
# =============================================================================
# Copy this file to .env.test and update with your test credentials
#
# IMPORTANT: The placeholder 'your-project' URLs will cause test failures!
# You MUST replace them with your actual Supabase project URL.
#
# To find your Supabase URL:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the "Project URL" (format: https://xxxx.supabase.co)
# =============================================================================

# =============================================================================
# TEST USER CREDENTIALS
# =============================================================================
# These credentials are used by TestSprite and Playwright for API/E2E tests.
# Create a test user in your Supabase project or use existing credentials.
TEST_USER_EMAIL=<your-test-email>
TEST_USER_PASSWORD=<your-test-password>

# =============================================================================
# APPLICATION URL
# =============================================================================
BASE_URL=http://localhost:3000

# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
# CRITICAL: Replace with your actual Supabase project URL and keys!
# DO NOT leave as 'your-project-id' or tests will fail with connection errors.
#
# Example: VITE_SUPABASE_URL=https://xkkhxhgkyavxcfgeojww.supabase.co
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side only (for regression scripts; do NOT prefix with VITE_)
# This key bypasses RLS - use only for admin/setup operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# AI configuration (for AI-powered features)
OPENROUTER_API_KEY=your-openrouter-key-here

# =============================================================================
# TESTSPRITE CONFIGURATION
# =============================================================================
# TestSprite will automatically read from this file when running API tests.
# Ensure all SUPABASE_* variables are properly configured before running tests.
